import { UniverseExperience } from '@/components/universe/UniverseExperience';

export const metadata = {
  title: 'Harmonic Universe | Choose Your Frequency',
  description: 'Boot Harmonic OS through the cup and heart, explore every frequency, and tune the living universe.',
};

export default function HarmonicUniversePage() {
  return <UniverseExperience />;
}
