'use client';

import { useMemo,useRef,useState,type CSSProperties,type PointerEvent as ReactPointerEvent,type ReactNode } from 'react';
import { useWorldCustomization,type WorldKey,type WorldMediaAsset } from '@/components/studio/WorldCustomizationProvider';
import {addStudioSection,createStudioSections,duplicateStudioSection,getStudioTree,getStyleStorageKey,isSectionVisibleAt,moveStudioSection,nestStudioSection,parseStudioSections,readElementStyle,removeStudioSection,reorderStudioSection,writeElementStyle,writeStylePatch,type StudioBreakpoint,type StudioElementStyle,type StudioSection,type StudioState} from '@/lib/creator-studio-model';

const worlds:Array<{key:WorldKey;label:string}>=[{key:'home',label:'Homepage'},{key:'melodic',label:'Melodic'},{key:'two-harmonic',label:'2 Harmonic'},{key:'fried-em',label:'Fried Em'},{key:'schmackinn',label:'Schmackinn'},{key:'global',label:'Global System'}];
const breakpoints:Array<{key:StudioBreakpoint;label:string;width:number}>=[{key:'desktop',label:'Desktop',width:1400},{key:'laptop',label:'Laptop',width:1100},{key:'tablet',label:'Tablet',width:820},{key:'mobile',label:'Mobile',width:390}];
const states:StudioState[]=['default','hover','pressed','focus'];
type DragState={id:string;startX:number;startY:number;originX:number;originY:number;originWidth:number;originHeight:number;mode:'move'|'resize'}|null;

export function VisualCanvasStudio(){
 const {settings,updateLabel,addMedia,updateMedia,removeMedia,duplicateMedia,replaceWorld}=useWorldCustomization();
 const [world,setWorld]=useState<WorldKey>('home');const [selected,setSelected]=useState<string|null>('hero');const [breakpoint,setBreakpoint]=useState<StudioBreakpoint>('desktop');const [state,setState]=useState<StudioState>('default');const [drag,setDrag]=useState<DragState>(null);const [draggedLayer,setDraggedLayer]=useState<string|null>(null);const [copyTarget,setCopyTarget]=useState<WorldKey>('melodic');const fileRef=useRef<HTMLInputElement>(null);
 const active=settings[world];const sections=useMemo(()=>parseStudioSections(active.labels.sectionStructure,world),[active.labels.sectionStructure,world]);const ordered=useMemo(()=>[...sections].sort((a,b)=>a.order-b.order),[sections]);const tree=useMemo(()=>getStudioTree(sections),[sections]);
 const selectedSection=selected&&!selected.startsWith('media:')?sections.find(s=>s.id===selected):undefined;const selectedMedia=selected?.startsWith('media:')?active.media.find(a=>`media:${a.id}`===selected):undefined;const selectedStyle=selectedSection?readElementStyle(active.labels,selectedSection.id,breakpoint,state):undefined;
 const pageTitle=active.labels['studio.pageTitle']||worlds.find(i=>i.key===world)?.label||world;const pageSlug=active.labels['studio.pageSlug']||world;const navVisible=active.labels['studio.navVisible']!=='false';const homePage=active.labels['studio.homePage']==='true'||world==='home';
 function saveSections(next:StudioSection[]){updateLabel(world,'sectionStructure',JSON.stringify(next));}
 function patchSection(id:string,patch:Partial<StudioSection>){saveSections(sections.map(s=>s.id===id?{...s,...patch}:s));}
 function writePatchFor(id:string,patch:Partial<StudioElementStyle>){const key=getStyleStorageKey(id,breakpoint,state);if(breakpoint==='desktop'&&state==='default'){const current=readElementStyle(active.labels,id);updateLabel(world,key,writeElementStyle({...current,...patch}));}else{let current:Partial<StudioElementStyle>={};try{current=JSON.parse(active.labels[key]||'{}')}catch{}updateLabel(world,key,writeStylePatch({...current,...patch}));}}
 function patchStyle(id:string,patch:Partial<StudioElementStyle>){writePatchFor(id,patch);const source=sections.find(s=>s.id===id);if(source?.componentKey&&source.componentLinked!==false){for(const sibling of sections)if(sibling.id!==id&&sibling.componentKey===source.componentKey&&sibling.componentLinked!==false)writePatchFor(sibling.id,patch);}}
 function startPointer(event:ReactPointerEvent<HTMLDivElement>,section:StudioSection,style:StudioElementStyle,mode:'move'|'resize'){event.stopPropagation();setSelected(section.id);if(section.locked||style.position!=='absolute'||state!=='default')return;event.currentTarget.setPointerCapture(event.pointerId);setDrag({id:section.id,startX:event.clientX,startY:event.clientY,originX:style.x,originY:style.y,originWidth:style.width,originHeight:style.minHeight,mode});}
 function movePointer(event:ReactPointerEvent<HTMLDivElement>){if(!drag)return;const canvas=event.currentTarget.closest('[data-studio-canvas]') as HTMLElement|null;const rect=canvas?.getBoundingClientRect();if(!rect)return;const dx=((event.clientX-drag.startX)/rect.width)*100,dy=((event.clientY-drag.startY)/rect.height)*100;if(drag.mode==='move')patchStyle(drag.id,{x:clamp(drag.originX+dx,0,100),y:clamp(drag.originY+dy,0,100)});else patchStyle(drag.id,{width:clamp(drag.originWidth+dx,5,100),minHeight:clamp(drag.originHeight+(event.clientY-drag.startY),24,1200)});}
 function stopPointer(event:ReactPointerEvent<HTMLDivElement>){if(event.currentTarget.hasPointerCapture(event.pointerId))event.currentTarget.releasePointerCapture(event.pointerId);setDrag(null);}
 function addSection(parentId?:string){const next=addStudioSection(sections,'section',parentId);saveSections(next);setSelected(next[next.length-1].id);}
 function duplicateSection(){if(!selectedSection)return;const next=duplicateStudioSection(sections,selectedSection.id);const copy=next.find(s=>s.id.startsWith(`${selectedSection.id}-copy-`));saveSections(next);if(copy){for(const bp of ['desktop','laptop','tablet','mobile'] as StudioBreakpoint[])updateLabel(world,getStyleStorageKey(copy.id,bp),writeElementStyle(readElementStyle(active.labels,selectedSection.id,bp)));setSelected(copy.id);}}
 function deleteSection(){if(!selectedSection)return;saveSections(removeStudioSection(sections,selectedSection.id));setSelected(null);}
 function duplicatePage(){replaceWorld(copyTarget,{...active,title:`${active.title||pageTitle} Copy`,labels:{...active.labels,'studio.pageTitle':`${pageTitle} Copy`,'studio.homePage':'false'},media:active.media.map(a=>({...a,id:crypto.randomUUID()}))});setWorld(copyTarget);setSelected(createStudioSections(copyTarget)[0]?.id||null);}
 async function upload(files:FileList|null){if(!files?.length)return;for(const file of Array.from(files))addMedia(world,{name:file.name,url:URL.createObjectURL(file),kind:file.type.startsWith('video/')?'video':'image',placement:'floating',x:50,y:50,width:30,height:30,opacity:100,rotation:0,zIndex:10,loop:true,muted:true,fit:'contain',motion:'none',duration:6,delay:0,radius:12,focalX:50,focalY:50,brightness:100,contrast:100,saturation:100,flipX:false,flipY:false,alt:file.name,link:'',visible:true,locked:false});}
 return <main className="min-h-screen bg-[#050308] text-white"><header className="sticky top-0 z-50 flex flex-wrap items-center justify-between gap-3 border-b border-white/10 bg-black/90 px-5 py-3 backdrop-blur-2xl"><div><p className="text-[10px] font-black uppercase tracking-[.28em] text-purple-300/60">Creator Studio · Editable Controls 10–13</p><h1 className="text-2xl font-black">Visual Canvas</h1></div><div className="flex flex-wrap gap-2">{worlds.map(item=><button key={item.key} onClick={()=>{setWorld(item.key);setSelected(createStudioSections(item.key)[0]?.id||null)}} className={`rounded-full px-3 py-2 text-xs font-black ${world===item.key?'bg-purple-200 text-black':'border border-white/10'}`}>{settings[item.key].labels['studio.pageTitle']||item.label}</button>)}</div></header>
 <div className="grid min-h-[calc(100vh-68px)] xl:grid-cols-[330px_1fr_420px]">
  <aside className="max-h-[calc(100vh-68px)] overflow-y-auto border-r border-white/10 bg-black/35 p-4"><Panel title="Page & Navigation" open><TextField label="Page name" value={pageTitle} onChange={v=>updateLabel(world,'studio.pageTitle',v)}/><TextField label="URL slug" value={pageSlug} onChange={v=>updateLabel(world,'studio.pageSlug',v.replace(/\s+/g,'-').toLowerCase())}/><TextField label="Navigation label" value={active.labels['studio.navLabel']||pageTitle} onChange={v=>updateLabel(world,'studio.navLabel',v)}/><TextField label="Dropdown parent" value={active.labels['studio.navParent']||''} onChange={v=>updateLabel(world,'studio.navParent',v)}/><Grid><NumberField label="Nav order" value={Number(active.labels['studio.navOrder']||0)} onChange={v=>updateLabel(world,'studio.navOrder',String(v))}/><NumberField label="Footer order" value={Number(active.labels['studio.footerOrder']||0)} onChange={v=>updateLabel(world,'studio.footerOrder',String(v))}/></Grid><Toggle label="Show in navigation" value={navVisible} onChange={v=>updateLabel(world,'studio.navVisible',String(v))}/><Toggle label="Show in footer" value={active.labels['studio.footerVisible']!=='false'} onChange={v=>updateLabel(world,'studio.footerVisible',String(v))}/><Toggle label="Set as homepage" value={homePage} onChange={v=>{for(const item of worlds)updateLabel(item.key,'studio.homePage',String(v&&item.key===world));}}/><TextField label="Page password" value={active.labels['studio.pagePassword']||''} onChange={v=>updateLabel(world,'studio.pagePassword',v)}/><div className="grid grid-cols-[1fr_120px] gap-2"><Select label="Duplicate page to" value={copyTarget} options={worlds.filter(i=>i.key!==world).map(i=>i.key)} onChange={v=>setCopyTarget(v as WorldKey)}/><button className="btn self-end" onClick={duplicatePage}>Duplicate</button></div></Panel>
  <div className="mb-3 flex items-center justify-between"><p className="text-[10px] font-black uppercase tracking-[.24em] text-white/35">Layers</p><button onClick={()=>addSection(selectedSection?.id)} className="rounded-lg border border-white/10 px-2 py-1 text-xs font-black">+ Child</button></div><div className="rounded-2xl border border-white/10 bg-white/[.025] p-2"><button onClick={()=>setSelected(null)} className="flex w-full items-center gap-2 rounded-xl px-2 py-2 text-left font-black"><span>▾</span><span>{pageTitle}</span><span className="ml-auto text-[10px] text-white/30">PAGE</span></button><div className="ml-2 border-l border-white/10 pl-2">{tree.map(({section,depth})=><LayerRow key={section.id} section={section} depth={depth} selected={selected===section.id} onSelect={()=>setSelected(section.id)} onVisible={()=>patchSection(section.id,{visible:!section.visible})} onLock={()=>patchSection(section.id,{locked:!section.locked})} onCollapse={()=>patchSection(section.id,{collapsed:!section.collapsed})} onDragStart={()=>setDraggedLayer(section.id)} onDrop={()=>{if(draggedLayer)saveSections(reorderStudioSection(sections,draggedLayer,section.id));setDraggedLayer(null)}}/>)}</div><div className="mt-2 border-t border-white/10 pt-2">{active.media.map(asset=><button key={asset.id} onClick={()=>setSelected(`media:${asset.id}`)} className={`flex w-full items-center gap-2 rounded-lg px-3 py-2 text-left text-xs ${selected===`media:${asset.id}`?'bg-purple-300/15':'text-white/55'}`}><span>{asset.kind==='video'?'▶':'▧'}</span><span className="truncate">{asset.name}</span></button>)}</div></div>
  {selectedSection&&<div className="mt-3 grid grid-cols-2 gap-2"><button onClick={()=>saveSections(moveStudioSection(sections,selectedSection.id,-1))} className="btn">Move Up</button><button onClick={()=>saveSections(moveStudioSection(sections,selectedSection.id,1))} className="btn">Move Down</button><button onClick={duplicateSection} className="btn">Duplicate</button><button onClick={deleteSection} className="btn text-red-200">Delete</button></div>}<button onClick={()=>fileRef.current?.click()} className="mt-4 w-full rounded-2xl border border-dashed border-purple-300/40 bg-purple-300/[.06] p-4 text-sm font-black">Upload Image / Video</button><input ref={fileRef} hidden type="file" multiple accept="image/*,video/*" onChange={e=>void upload(e.target.files)}/></aside>
  <section className="relative overflow-auto bg-[#09060d] p-5"><div className="mb-4 flex flex-wrap items-center justify-center gap-2">{breakpoints.map(item=><button key={item.key} onClick={()=>setBreakpoint(item.key)} className={`rounded-full px-4 py-2 text-xs font-black ${breakpoint===item.key?'bg-cyan-200 text-black':'border border-white/10'}`}>{item.label}</button>)}<span className="mx-2 h-5 w-px bg-white/10"/>{states.map(item=><button key={item} onClick={()=>setState(item)} className={`rounded-full px-3 py-2 text-xs font-black capitalize ${state===item?'bg-pink-200 text-black':'border border-white/10'}`}>{item}</button>)}</div><div data-studio-canvas onPointerDown={()=>setSelected(null)} className="relative mx-auto min-h-[900px] overflow-hidden rounded-[2rem] border border-white/10 transition-[width]" style={{width:'100%',maxWidth:breakpoints.find(i=>i.key===breakpoint)?.width,background:active.background,color:active.text,boxShadow:`0 0 60px ${active.glow}22`}}><div className="pointer-events-none absolute inset-0 opacity-25 [background-image:linear-gradient(rgba(255,255,255,.04)_1px,transparent_1px),linear-gradient(90deg,rgba(255,255,255,.04)_1px,transparent_1px)] [background-size:24px_24px]"/>{active.media.filter(a=>a.visible!==false).map(asset=><MediaLayer key={asset.id} asset={asset} selected={selected===`media:${asset.id}`} onSelect={()=>setSelected(`media:${asset.id}`)} onChange={p=>updateMedia(world,asset.id,p)}/>) }{ordered.filter(s=>isSectionVisibleAt(s,breakpoint)).map(section=>{const style=readElementStyle(active.labels,section.id,breakpoint,state);const isSelected=selected===section.id;return <div id={`studio-${section.id}`} key={section.id} onPointerDown={e=>startPointer(e,section,style,'move')} onPointerMove={movePointer} onPointerUp={stopPointer} onPointerCancel={stopPointer} className={`${style.position==='absolute'?'absolute':'relative'} select-none ${section.locked||state!=='default'?'cursor-default':'cursor-move'} ${isSelected?'ring-2 ring-purple-200/70':''}`} style={canvasStyle(style)}><p className="text-[10px] font-black uppercase tracking-[.2em]" style={{color:active.secondary}}>{section.kind}{section.componentKey?` · ${section.componentKey}:${section.componentVariant||'default'}`:''}</p><h2 className="mt-2 text-[1.65em] font-black">{section.label}</h2><p className="mt-2 opacity-70">Full hierarchy, linked components, responsive visibility, and page controls.</p>{isSelected&&!section.locked&&style.position==='absolute'&&state==='default'&&<div onPointerDown={e=>startPointer(e,section,style,'resize')} onPointerMove={movePointer} onPointerUp={stopPointer} onPointerCancel={stopPointer} className="absolute -bottom-2 -right-2 h-5 w-5 cursor-se-resize rounded-full bg-purple-200 ring-4 ring-black"/>}</div>})}</div></section>
  <aside className="max-h-[calc(100vh-68px)] overflow-y-auto border-l border-white/10 bg-black/40 p-4"><p className="text-[10px] font-black uppercase tracking-[.24em] text-white/35">Properties · {breakpoint} · {state}</p>{!selectedSection&&!selectedMedia&&<p className="mt-4 text-sm text-white/45">Select a section or media layer.</p>}{selectedSection&&selectedStyle&&<SectionProperties section={selectedSection} sections={sections} breakpoint={breakpoint} style={selectedStyle} patchSection={p=>patchSection(selectedSection.id,p)} patchStyle={p=>patchStyle(selectedSection.id,p)} onParent={parentId=>saveSections(nestStudioSection(sections,selectedSection.id,parentId||undefined))}/>} {selectedMedia&&<MediaProperties media={selectedMedia} onChange={p=>updateMedia(world,selectedMedia.id,p)} onDuplicate={()=>duplicateMedia(world,selectedMedia.id)} onRemove={()=>{removeMedia(world,selectedMedia.id);setSelected(null)}}/>}</aside>
 </div></main>;
}

function SectionProperties({section,sections,breakpoint,style,patchSection,patchStyle,onParent}:{section:StudioSection;sections:StudioSection[];breakpoint:StudioBreakpoint;style:StudioElementStyle;patchSection:(p:Partial<StudioSection>)=>void;patchStyle:(p:Partial<StudioElementStyle>)=>void;onParent:(id:string)=>void}){const interaction=section.interaction??{action:'none' as const,target:'',newTab:false};const bpVisible=section.breakpointVisibility?.[breakpoint]??true;return <><TextField label="Layer name" value={section.label} onChange={label=>patchSection({label})}/><Grid><button className="btn" onClick={()=>patchSection({visible:!section.visible})}>{section.visible?'Hide all':'Show all'}</button><button className="btn" onClick={()=>patchSection({locked:!section.locked})}>{section.locked?'Unlock':'Lock'}</button></Grid><Panel title="Hierarchy" open><Select label="Parent layer" value={section.parentId||''} options={['',...sections.filter(s=>s.id!==section.id).map(s=>s.id)]} onChange={onParent}/><Toggle label={`Visible on ${breakpoint}`} value={bpVisible} onChange={value=>patchSection({breakpointVisibility:{...section.breakpointVisibility,[breakpoint]:value}})}/></Panel><Panel title="Position & Size" open><Select label="Position" value={style.position} options={['absolute','relative','fixed','sticky']} onChange={v=>patchStyle({position:v as StudioElementStyle['position']})}/><Grid><NumberField label="X" value={style.x} onChange={x=>patchStyle({x})}/><NumberField label="Y" value={style.y} onChange={y=>patchStyle({y})}/><NumberField label="Width" value={style.width} onChange={width=>patchStyle({width})}/><NumberField label="Height" value={style.minHeight} onChange={minHeight=>patchStyle({minHeight})}/><NumberField label="Z index" value={style.zIndex} onChange={zIndex=>patchStyle({zIndex})}/><NumberField label="Radius" value={style.radius} onChange={radius=>patchStyle({radius})}/></Grid></Panel><Panel title="Spacing & Layout"><Grid><NumberField label="Pad top" value={style.paddingTop} onChange={paddingTop=>patchStyle({paddingTop})}/><NumberField label="Pad right" value={style.paddingRight} onChange={paddingRight=>patchStyle({paddingRight})}/><NumberField label="Pad bottom" value={style.paddingBottom} onChange={paddingBottom=>patchStyle({paddingBottom})}/><NumberField label="Pad left" value={style.paddingLeft} onChange={paddingLeft=>patchStyle({paddingLeft})}/></Grid><Select label="Layout" value={style.layout} options={['free','flex','grid']} onChange={v=>patchStyle({layout:v as StudioElementStyle['layout']})}/><Grid><NumberField label="Gap" value={style.gap} onChange={gap=>patchStyle({gap})}/><NumberField label="Columns" value={style.columns} onChange={columns=>patchStyle({columns})}/></Grid></Panel><Panel title="Typography & Appearance"><Grid><NumberField label="Font size" value={style.fontSize} onChange={fontSize=>patchStyle({fontSize})}/><NumberField label="Weight" value={style.fontWeight} onChange={fontWeight=>patchStyle({fontWeight})}/><NumberField label="Opacity" value={style.opacity} onChange={opacity=>patchStyle({opacity})}/><NumberField label="Glow" value={style.glow} onChange={glow=>patchStyle({glow})}/></Grid><ColorField label="Text" value={style.textColor} onChange={textColor=>patchStyle({textColor})}/><ColorField label="Background" value={style.backgroundColor} onChange={backgroundColor=>patchStyle({backgroundColor,backgroundType:'solid'})}/></Panel><Panel title="Component"><TextField label="Component name" value={section.componentKey||''} onChange={componentKey=>patchSection({componentKey:componentKey||undefined,componentLinked:Boolean(componentKey)})}/><TextField label="Variant" value={section.componentVariant||'default'} onChange={componentVariant=>patchSection({componentVariant})}/><Toggle label="Linked instance" value={section.componentLinked!==false&&Boolean(section.componentKey)} onChange={componentLinked=>patchSection({componentLinked})}/><p className="text-xs text-white/40">Linked instances with the same component name receive the same style edits. Turn linking off to detach this instance.</p></Panel><Panel title="Interaction"><Select label="On click" value={interaction.action} options={['none','link','scroll','toggle','show','hide']} onChange={action=>patchSection({interaction:{...interaction,action:action as typeof interaction.action}})}/><TextField label="Target URL or layer ID" value={interaction.target} onChange={target=>patchSection({interaction:{...interaction,target}})}/><Toggle label="Open link in new tab" value={interaction.newTab} onChange={newTab=>patchSection({interaction:{...interaction,newTab}})}/></Panel></>}
function MediaProperties({media,onChange,onDuplicate,onRemove}:{media:WorldMediaAsset;onChange:(p:Partial<WorldMediaAsset>)=>void;onDuplicate:()=>void;onRemove:()=>void}){return <><TextField label="Media name" value={media.name} onChange={name=>onChange({name})}/><Grid><button className="btn" onClick={()=>onChange({visible:media.visible===false})}>{media.visible===false?'Show':'Hide'}</button><button className="btn" onClick={()=>onChange({locked:!media.locked})}>{media.locked?'Unlock':'Lock'}</button></Grid><Panel title="Media Transform" open><Grid><NumberField label="X" value={media.x} onChange={x=>onChange({x})}/><NumberField label="Y" value={media.y} onChange={y=>onChange({y})}/><NumberField label="Width" value={media.width} onChange={width=>onChange({width})}/><NumberField label="Height" value={media.height??30} onChange={height=>onChange({height})}/><NumberField label="Rotation" value={media.rotation} onChange={rotation=>onChange({rotation})}/><NumberField label="Z index" value={media.zIndex} onChange={zIndex=>onChange({zIndex})}/></Grid><Select label="Fit" value={media.fit} options={['contain','cover']} onChange={fit=>onChange({fit:fit as 'contain'|'cover'})}/></Panel><Panel title="Crop & Filters"><Grid><NumberField label="Focal X" value={media.focalX??50} onChange={focalX=>onChange({focalX})}/><NumberField label="Focal Y" value={media.focalY??50} onChange={focalY=>onChange({focalY})}/><NumberField label="Corners" value={media.radius??12} onChange={radius=>onChange({radius})}/><NumberField label="Opacity" value={media.opacity} onChange={opacity=>onChange({opacity})}/><NumberField label="Brightness" value={media.brightness??100} onChange={brightness=>onChange({brightness})}/><NumberField label="Contrast" value={media.contrast??100} onChange={contrast=>onChange({contrast})}/></Grid></Panel><Panel title="Accessibility & Link"><TextField label="Alt text" value={media.alt||''} onChange={alt=>onChange({alt})}/><TextField label="Link" value={media.link||''} onChange={link=>onChange({link})}/></Panel><Grid><button className="btn" onClick={onDuplicate}>Duplicate</button><button className="btn text-red-200" onClick={onRemove}>Remove</button></Grid></>}
function MediaLayer({asset,selected,onSelect,onChange}:{asset:WorldMediaAsset;selected:boolean;onSelect:()=>void;onChange:(p:Partial<WorldMediaAsset>)=>void}){const[drag,setDrag]=useState<{x:number;y:number;clientX:number;clientY:number}|null>(null);function start(e:ReactPointerEvent<HTMLDivElement>){e.stopPropagation();onSelect();if(asset.locked)return;e.currentTarget.setPointerCapture(e.pointerId);setDrag({x:asset.x,y:asset.y,clientX:e.clientX,clientY:e.clientY})}function move(e:ReactPointerEvent<HTMLDivElement>){if(!drag)return;const rect=(e.currentTarget.closest('[data-studio-canvas]') as HTMLElement|null)?.getBoundingClientRect();if(!rect)return;onChange({x:clamp(drag.x+((e.clientX-drag.clientX)/rect.width)*100,0,100),y:clamp(drag.y+((e.clientY-drag.clientY)/rect.height)*100,0,100)})}function stop(e:ReactPointerEvent<HTMLDivElement>){if(e.currentTarget.hasPointerCapture(e.pointerId))e.currentTarget.releasePointerCapture(e.pointerId);setDrag(null)}const mediaStyle:CSSProperties={width:'100%',height:'100%',objectFit:asset.fit,objectPosition:`${asset.focalX??50}% ${asset.focalY??50}%`,borderRadius:asset.radius??12,filter:`brightness(${asset.brightness??100}%) contrast(${asset.contrast??100}%) saturate(${asset.saturation??100}%)`};return <div onPointerDown={start} onPointerMove={move} onPointerUp={stop} onPointerCancel={stop} className={`absolute ${asset.locked?'cursor-default':'cursor-move'} ${selected?'ring-2 ring-purple-200':''}`} style={{left:`${asset.x}%`,top:`${asset.y}%`,width:`${asset.width}%`,height:asset.height?`${asset.height}%`:'auto',opacity:asset.opacity/100,zIndex:asset.zIndex,transform:`translate(-50%,-50%) rotate(${asset.rotation}deg) scaleX(${asset.flipX?-1:1}) scaleY(${asset.flipY?-1:1})`}}>{asset.kind==='video'?<video src={asset.url} autoPlay loop={asset.loop} muted={asset.muted} style={mediaStyle}/>:<img src={asset.url} alt={asset.alt||asset.name} style={mediaStyle}/>}</div>}
function canvasStyle(s:StudioElementStyle):CSSProperties{const justify={start:'flex-start',center:'center',end:'flex-end',between:'space-between',around:'space-around',evenly:'space-evenly'} as const;const align={start:'flex-start',center:'center',end:'flex-end',stretch:'stretch'} as const;const background=s.backgroundType==='transparent'?'transparent':s.backgroundType==='gradient'?`linear-gradient(${s.gradientAngle}deg,${s.gradientFrom},${s.gradientTo})`:s.backgroundColor;return{left:s.position==='absolute'?`${s.x}%`:undefined,top:s.position==='absolute'?`${s.y}%`:undefined,transform:s.position==='absolute'?'translate(-50%,-50%)':undefined,zIndex:s.zIndex,width:`${s.width}${s.widthUnit}`,minWidth:s.minWidth||undefined,maxWidth:s.maxWidth||undefined,minHeight:s.minHeight,maxHeight:s.maxHeight||undefined,margin:`${s.marginTop}px ${s.marginRight}px ${s.marginBottom}px ${s.marginLeft}px`,padding:`${s.paddingTop}px ${s.paddingRight}px ${s.paddingBottom}px ${s.paddingLeft}px`,display:s.layout==='free'?'block':s.layout,flexDirection:s.direction,flexWrap:s.wrap?'wrap':'nowrap',alignItems:align[s.align],justifyContent:justify[s.justify],gap:s.gap,gridTemplateColumns:s.layout==='grid'?`repeat(${Math.max(1,s.columns)},minmax(0,1fr))`:undefined,overflow:s.overflow,background,border:`${s.borderWidth}px ${s.borderStyle} ${s.borderColor}`,borderRadius:s.radius,opacity:s.opacity/100,filter:`blur(${s.blur}px)`,boxShadow:`${s.shadowX}px ${s.shadowY}px ${s.shadowBlur}px ${s.shadowSpread}px ${s.shadowColor},0 0 ${s.glow}px ${s.borderColor}`,fontFamily:s.fontFamily,fontSize:s.fontSize,fontWeight:s.fontWeight,lineHeight:s.lineHeight,letterSpacing:s.letterSpacing,textAlign:s.textAlign,color:s.textColor,textTransform:s.textTransform}}
function LayerRow({section,depth,selected,onSelect,onVisible,onLock,onCollapse,onDragStart,onDrop}:{section:StudioSection;depth:number;selected:boolean;onSelect:()=>void;onVisible:()=>void;onLock:()=>void;onCollapse:()=>void;onDragStart:()=>void;onDrop:()=>void}){return <div draggable onDragStart={onDragStart} onDragOver={e=>e.preventDefault()} onDrop={e=>{e.preventDefault();onDrop()}} className={`mb-1 flex items-center rounded-xl ${selected?'bg-purple-300/15':'hover:bg-white/5'}`} style={{marginLeft:depth*14}}><button onClick={onCollapse} className="p-2 text-xs">{section.collapsed?'▸':'▾'}</button><button onClick={onSelect} className="flex min-w-0 flex-1 items-center gap-2 py-2 text-left"><span className="text-white/25">⋮⋮</span><span className="truncate text-sm font-bold">{section.label}</span>{section.componentKey&&<span className="text-[9px] text-cyan-200">◆</span>}</button><button onClick={onLock} className="p-2 text-xs">{section.locked?'🔒':'🔓'}</button><button onClick={onVisible} className="p-2 text-xs">{section.visible?'◉':'○'}</button></div>}
function Panel({title,children,open=false}:{title:string;children:ReactNode;open?:boolean}){return <details open={open} className="my-4 rounded-2xl border border-white/10 bg-white/[.025] p-3"><summary className="cursor-pointer text-xs font-black uppercase tracking-wider text-white/55">{title}</summary><div className="mt-3 grid gap-3">{children}</div></details>}
function Grid({children}:{children:ReactNode}){return <div className="grid grid-cols-2 gap-2">{children}</div>}
function NumberField({label,value,onChange}:{label:string;value:number;onChange:(v:number)=>void}){return <label className="grid gap-1 text-[10px] font-black uppercase tracking-wider text-white/40">{label}<input type="number" value={Number.isFinite(value)?value:0} onChange={e=>onChange(Number(e.target.value))} className="rounded-lg border border-white/10 bg-black p-2 text-sm text-white"/></label>}
function TextField({label,value,onChange}:{label:string;value:string;onChange:(v:string)=>void}){return <label className="grid gap-1 text-[10px] font-black uppercase tracking-wider text-white/40">{label}<input value={value} onChange={e=>onChange(e.target.value)} className="rounded-lg border border-white/10 bg-black p-2 text-sm text-white"/></label>}
function ColorField({label,value,onChange}:{label:string;value:string;onChange:(v:string)=>void}){return <label className="flex items-center justify-between text-xs font-black uppercase text-white/40"><span>{label}</span><input type="color" value={normalizeColor(value)} onChange={e=>onChange(e.target.value)} className="h-9 w-14 rounded border-0 bg-transparent"/></label>}
function Select({label,value,options,onChange}:{label:string;value:string;options:string[];onChange:(v:string)=>void}){return <label className="grid gap-1 text-[10px] font-black uppercase tracking-wider text-white/40">{label}<select value={value} onChange={e=>onChange(e.target.value)} className="rounded-lg border border-white/10 bg-black p-2 text-sm text-white">{options.map(option=><option key={option} value={option}>{option||'None'}</option>)}</select></label>}
function Toggle({label,value,onChange}:{label:string;value:boolean;onChange:(v:boolean)=>void}){return <label className="flex items-center justify-between text-xs font-black text-white/55"><span>{label}</span><input type="checkbox" checked={value} onChange={e=>onChange(e.target.checked)}/></label>}
function clamp(v:number,min:number,max:number){return Math.max(min,Math.min(max,v))}function normalizeColor(v:string){return /^#[0-9a-fA-F]{6}$/.test(v)?v:'#ffffff'}
